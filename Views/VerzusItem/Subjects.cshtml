
<h2>@ViewBag.Title</h2>

@foreach (var item in ViewBag.VerzusItems) {
    <span class="verzus-item"><a href="@Url.Action("Search", "Verzus", new {itemContent=item.ItemContent})" title="">@item.ItemContent</a></span>
}